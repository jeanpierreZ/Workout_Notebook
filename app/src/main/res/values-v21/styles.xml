<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- EditButton Theme -->
    <style name="EditButtonTheme" parent="Widget.MaterialComponents.Button">
        <item name="icon">@drawable/ic_baseline_edit_24</item>
        <item name="android:layout_marginStart">@dimen/edit_button_margin</item>
        <item name="android:layout_marginEnd">@dimen/edit_button_margin</item>
        <item name="android:backgroundTint">@color/colorPrimaryDark</item>
        <item name="android:textSize">@dimen/subtitle_text_size</item>
    </style>

    <!-- StartTrainingSessionButton Theme -->
    <style name="StartTrainingSessionButtonTheme" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/colorAccentLight</item>
        <item name="android:layout_marginStart">@dimen/edit_button_margin</item>
        <item name="android:layout_marginEnd">@dimen/edit_button_margin</item>
        <item name="android:textSize">@dimen/subtitle_text_size</item>
        <item name="android:textColor">@color/colorTextPrimary</item>
        <item name="cornerRadius">@dimen/fab_margin</item>
    </style>
</resources>